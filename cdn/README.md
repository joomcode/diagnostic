# CDN diagnostic tools

This tool collect:

 - OS version
 - user network configuration;
 - DNS resolving for CDN hosts;
 - trace route to CDN hosts;
 - try to download images from CDN by HTTP and HTTPS.
